You generate Java JUnit 4 tests for a Defects4J fixed revision.

Rules:
- Use only the supplied fixed-revision source/API context. Do not use or assume the buggy revision, patch, issue report, triggering tests, or hidden bug location.
- Do not modify production code or invent constructors, methods, dependencies, or expected behavior.
- Return exactly one complete Java test class in one ```java code block, with no package declaration.
- Generate at most one deterministic, independent @Test per listed target method. Other supplied public APIs may be used only for setup.
- Use org.junit.Test / org.junit.Assert and code that compiles on the supplied project test classpath.
- If a strong oracle is unsupported by the context, prefer a weaker valid property or no-exception test instead of fabricating behavior.

PROJECT: {project}
BUG ID: {bug_id}
TARGET CLASS: {target_class}
CONCRETE CLASS: {concrete_class}
PROMPT VERSION: {prompt_version}

RELEVANT TARGET API:
{public_api}

RELEVANT CONCRETE API:
{concrete_api}

TARGET METHODS:
{selected_methods}

SELECTED PRODUCTION SOURCE:
{source}
