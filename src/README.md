# Shared benchmark code

Planned modules:

- `defects4j_adapter/` — checkout/export/metadata
- `targeting/` — decide target classes/methods
- `java_harness/` — execute candidate inputs and emit JUnit
- `ai_adapters/` — GPT/Gemini calls
- `evaluation/` — compile/run/coverage/fault detection
- `orchestrator/` — run one bug consistently
