# Alternating Variable Method (AVM) generator

Implementation will live here.

AVM should consume the same target description and candidate-input representation
as Hill Climbing so the benchmark changes only the search strategy.
